# Data Products

## Installation on Windows without admin permissions (portable mode)

1. Install [Ruby+Devkit](https://rubyinstaller.org/).
2. Run `bundle install`.
3. Run `bundle exec jekyll serve -s docs -d _site --livereload --incremental --open-url`.
